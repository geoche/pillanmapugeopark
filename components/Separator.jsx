const Separator = () => {
    
    return (
        <div className={`bg-white w-[90%] items-center mx-auto`}>
            <hr className={` relative h-0.5 border-t-0 max-w-screen-xl mx-auto bg-black`}/>
        </div>
    );
};

export default Separator;
