const Separator = () => {
    
    return (
        <div className={`bg-default`}>
            <hr className={` relative my-4 h-0.5 border-t-0 max-w-screen-xl mx-auto bg-white )}`}/>
        </div>
    );
};

export default Separator;
