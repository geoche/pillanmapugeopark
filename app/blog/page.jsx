import BlogGrid from "@components/blog/BlogGrid";

const Blog = () => {
    return (
        <section className={`component-section`}>
            <BlogGrid/>
        </section>
    );
};

export default Blog;
